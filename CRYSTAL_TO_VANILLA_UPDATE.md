# Crystal -> Vanilla

This build keeps the existing Lower Pixel Regular website and normalizes
incoming Crystal results to the existing `vanilla` leaderboard key.

It also migrates any already-stored `crystal` rankings/tests to `vanilla`
when the database is initialized.
